# ============================================
# SUPABASE CONFIGURATION
# ============================================
# Your Supabase project URL (found in project settings)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anonymous Key (public, safe to expose in frontend)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here


# ============================================
# IMPORTANT SECURITY NOTES
# ============================================
# 1. Never commit .env.local or .env files to git
# 2. SUPABASE_SERVICE_ROLE_KEY should ONLY be in .env.local (not in .env)
# 3. For production deployment, set these in your hosting platform's environment variables
# 4. The .gitignore file is configured to exclude all .env* files


# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Copy this file to .env.local
# 2. Replace the placeholder values with your actual Supabase credentials
# 3. For service role key, copy .env.local.example to .env.local and add:
#    SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
# 4. Run database migrations from /supabase folder
# 5. Create admin user: node --env-file=.env.local scripts/create-admin.js
